git checkout -b dev       # devブランチ作成 & 切り替え
# devで作業
git add .
git commit -m "feat: 新機能追加"
git push -u origin dev    # GitHubにdevブランチをpush

# 作業完了したらmainに戻ってマージ
git checkout main
git merge dev
git push
